import styled from '@emotion/styled'  

export const GalleryList = styled.ul` 
    display: flex; 
    flex-wrap: wrap;
    gap: 15px; 
    justify-content: center;
    margin: 20px 0;
    list-style: none; 
`